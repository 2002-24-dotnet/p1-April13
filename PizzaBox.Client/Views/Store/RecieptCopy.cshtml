@model PizzaBox.Domain.Models.Order

<h1>Order #@Model.Id</h1>

<p>
  Customer: @Model.User.Name <br/>
  Submitted: @Html.DisplayTextFor(o => o.date)
</p>

<p>
  Pizzas Ordered <br/>
  <ul>
    @foreach (var p in @Model.Pizzas)
    {
      <li>@p.Name $@p.Price</li>
    }
  </ul>
</p>
<p>Total: $@Model.Price</p>

@Html.ActionLink("Back to Order History", "StoreHistory", new { id = @Model.Store.Id })
<a href="/account/logout">Logout</a>